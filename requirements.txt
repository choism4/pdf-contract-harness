# pdf-contract-writer — Python 3.14, .venv
# 핵심: pypdfium2(렌더·벡터 path·char box) + fastapi/uvicorn(웹) + Pillow(이미지)
pypdfium2==5.9.0
fastapi==0.136.3
uvicorn==0.49.0
pillow==12.2.0
python-multipart==0.0.32

# 보조: 추출 fallback / 스캔 OCR
pdfplumber==0.11.9      # lines/rects/edges fallback (주력은 pypdfium2 raw)
pypdf==6.13.2           # AcroForm 양식필드 추출용
pytesseract==0.3.13     # 스캔 PDF OCR (tesseract kor+eng 별도 설치 필요)
