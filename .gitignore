# Python venv
.venv/
__pycache__/
*.pyc

# OS
.DS_Store

# User working projects — only the bundled sample ships in the repo
projects/*/
!projects/sample-employment-contract/

# Generated page renders are never committed (regenerated from source.pdf)
projects/**/page-*.png
projects/**/*.overlay.png
# …except the bundled sample's render, so the demo works on first clone
!projects/sample-employment-contract/page-0.png
